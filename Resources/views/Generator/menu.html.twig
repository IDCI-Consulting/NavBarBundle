<div id="idci_nav_bar">
  <div class="container">
    <div class="brand active">
      <a href="{{ menu.brand_url }}" alt="{{ menu.brand_title }}" title="{{ menu.brand_title }}">
        {{ menu.brand_title }}
        <p>{{ menu.brand_description }}</p>
      </a>
    </div>
    <ul>
      {% for k, item in menu.items %}
      <li id="idci_nav_bar_item_{{ k }}">
        <a href="{{ item.url }}" alt="{{ item.title }}" title="{{ item.title }}"{% if item.target %} target="{{ item.target }}"{% endif %}>
            {{ item.title }}
        </a>
      </li>
      {% endfor %}
    </ul>
  </div>
</div>

<script type="text/javascript">
var $ = document;
var cssId = 'idci_nav_bar_style_id';
if (!$.getElementById(cssId)) {
    var head  = $.getElementsByTagName('head')[0];
    var link  = $.createElement('link');
    link.id   = cssId;
    link.rel  = 'stylesheet';
    link.type = 'text/css';
    link.href = '{{ url('idci_navbar_menu_nav', { '_format': 'css' }, true) }}';
    link.media = 'all';
    head.appendChild(link);
}
</script>
